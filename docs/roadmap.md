# Roadmap

The canonical roadmap lives at [/ROADMAP.md](../ROADMAP.md) (repo root), not here — this
file exists only so the documented docs/ set matches the project's doc index; see
[docs/release.md](release.md) for how roadmap status connects to releases. Duplicating the
session/status table in two places would just let them drift out of sync (see the "no
duplicate docs" rule in [CONTRIBUTING.md](../CONTRIBUTING.md)).
